# Скопируйте этот файл в .env.local и заполните значения.
# .env.local в .gitignore — в репозиторий не попадёт.

# Anthropic API key для AI-ассистента.
# Получить: https://console.anthropic.com/ → Settings → API Keys
ANTHROPIC_API_KEY=
